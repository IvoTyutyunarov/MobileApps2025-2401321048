<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <!-- AppBarLayout за горната лента -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Самата лента с инструменти (Toolbar) -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/teal_700"
            app:title="Simple Notes"
            app:titleTextColor="@android:color/white" />
        <!-- Премахнах app:layout_behavior оттук, защото не е нужен -->

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Списъкът с бележки -->
    <ListView
        android:id="@+id/listViewNotes"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="0.5dp"
        android:padding="8dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"/>
    <!-- Атрибутът тук е правилен и казва на ListView да е под лентата -->
    <!-- Премахнах android:layout_above, защото не е нужен -->

    <!-- Кръгъл бутон за добавяне на нова бележка -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:contentDescription="@string/add_note"
        app:srcCompat="@android:drawable/ic_input_add"
        app:tint="@android:color/white"
        app:backgroundTint="@color/teal_700" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
